---
id: local.backend-no-silent-memory-fallback
title: 后端禁止静默回退到内存存储
language: go
level: warn
status: draft
tags: [local, go, config, runtime]
---

# 后端禁止静默回退到内存存储

后端运行时启动必须从环境变量读取明确的数据源配置，并在缺失时立即失败。内存存储只能作为测试夹具或显式选择的开发工具，不能作为生产运行时的静默回退路径。

```grit
// TODO: 等数据源构造路径足够稳定、可以识别回退分支且不误报测试代码后，再补充 GritQL。
```

## 反例

```go
if databaseURL == "" {
  store = memory.NewStore()
}
```

## 正例

```go
if databaseURL == "" {
  return Config{}, errors.New("DATABASE_URL is required")
}
```
